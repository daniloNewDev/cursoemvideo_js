let v = function(x) {
        return x * 2
    }
    //É possível atribuir uma FUNÇÃO ao valor de uma variável.
console.log(v(5))
    //No exemplo acima foi possível passar para a variável um parâmetro que executa uma função, nesse caso uma função de soma.